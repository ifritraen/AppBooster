package com.raen.optidroid.presentation.ui.theme


import androidx.compose.ui.unit.dp

/**
 * Default value of 1 Density-independent pixel (dp).
 */
val defaultOneDp = 1.dp;

/**
 * Default value of 2 Density-independent pixels (dp).
 */
val defaultTwoDp = 2.dp;

/**
 * Default value of 3 Density-independent pixels (dp).
 */
val defaultThreeDp = 3.dp;

/**
 * Padding value of 0 Density-independent pixels (dp).
 */
val paddingZero = 0.dp;

/**
 * Extra small padding value of 4 Density-independent pixels (dp).
 */
val paddingXSmall = 4.dp;

/**
 * Super small padding value of 6 Density-independent pixels (dp).
 */
val paddingSSmall = 6.dp;

/**
 * Small padding value of 8 Density-independent pixels (dp).
 */
val paddingSmall = 8.dp;

/**
 * Extra medium padding value of 12 Density-independent pixels (dp).
 */
val paddingXMedium = 12.dp;

/**
 * Medium padding value of 16 Density-independent pixels (dp).
 */
val paddingMedium = 16.dp;

/**
 * Large padding value of 24 Density-independent pixels (dp).
 */
val paddingLarge = 24.dp;

/**
 * Extra large padding value of 32 Density-independent pixels (dp).
 */
val paddingXLarge = 32.dp;

/**
 * Extra extra large padding value of 48 Density-independent pixels (dp).
 */
val paddingXXLarge = 48.dp;

/**
 * Size of button icons, set to 20 Density-independent pixels (dp).
 */
val buttonIconSize = 20.dp;

/**
 * Small icon size, set to 12 Density-independent pixels (dp).
 */
val iconSizeSmall = 12.dp;

/**
 * Icon size for actions, set to 16 Density-independent pixels (dp).
 */
val iconSizeAction = 16.dp;

/**
 * Medium icon size, set to 24 Density-independent pixels (dp).
 */
val iconSizeMedium = 24.dp;

/**
 * Extra large medium icon size, set to 40 Density-independent pixels (dp).
 */
val iconSizeXLMedium = 40.dp;

/**
 * Large medium icon size, set to 40 Density-independent pixels (dp).
 */
val iconSizeLMedium = 32.dp;

/**
 * Large icon size, set to 48 Density-independent pixels (dp).
 */
val iconSizeLarge = 48.dp;

/**
 * Extra large icon size, set to 64 Density-independent pixels (dp).
 */
val iconSizeXLarge = 64.dp;

/**
 * Icon size for movement category, set to 40 Density-independent pixels (dp).
 */
val iconSizeMovementCategory = 40.dp;

/**
 * Extra small corner shape with a radius of 4 Density-independent pixels (dp).
 */
val cornerShapeXSmall = 4.dp;

/**
 * Small corner shape with a radius of 8 Density-independent pixels (dp).
 */
val cornerShapeSmall = 8.dp;

/**
 * Medium corner shape with a radius of 16 Density-independent pixels (dp).
 */
val cornerShapeMedium = 16.dp;

/**
 * Very large corner shape with a radius of 98 Density-independent pixels (dp).
 */
val cornerShape5XL = 98.dp;

/**
 * Height of the slider bar, set to 8 Density-independent pixels (dp).
 */
val sliderBarHeight = 8.dp;

/**
 * Height of the standard button, set to 48 Density-independent pixels (dp).
 */
val buttonSizeBig = 48.dp;